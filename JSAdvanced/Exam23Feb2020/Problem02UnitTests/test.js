//let OBJ = require('./asdasdads');
let assert = require('chai').assert;

describe('asd', () => {
    it('asdd', () => {
        assert.equal(true, true);
    });
});


describe('dsa', () => {
    it('dsaa', () => {
        assert.equal(true, true);
    });
});