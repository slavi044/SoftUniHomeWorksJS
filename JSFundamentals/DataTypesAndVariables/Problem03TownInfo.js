function townInfo(town, population, area) {
    let stringToPrint = `Town ${town} has population of ${population} and area ${area} square km.`;
    console.log(stringToPrint);
    
}

townInfo('Sofia', 1286383, 492);